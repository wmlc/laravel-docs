{
  "engines": {
    "node": ">=20.12.0"
  },
  "devDependencies": {
    "vitepress": "^2.0.0-alpha.20"
  },
  "scripts": {
    "dev": "DOCS_VERSION=13 vitepress dev",
    "dev:12": "DOCS_VERSION=12 vitepress dev",
    "dev:13": "DOCS_VERSION=13 vitepress dev",
    "build": "rm -rf .vitepress/dist && npm run build:12 && npm run build:13",
    "build:12": "DOCS_VERSION=12 vitepress build",
    "build:13": "DOCS_VERSION=13 vitepress build",
    "preview": "vitepress preview",
    "serve": "npm run build && node serve.mjs"
  }
}